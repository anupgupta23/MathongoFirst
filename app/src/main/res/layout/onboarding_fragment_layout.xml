<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    
    <android.support.v4.view.ViewPager
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/onboardingFragmentLayoutViewPager"/>

    <android.support.design.widget.TabLayout
        android:layout_width="match_parent"
        android:layout_height="15dp"
        app:tabIndicatorHeight="0dp"
        app:tabGravity="center"
        app:tabBackground="@drawable/tab_selector"
        android:id="@+id/onboardingFragmentLayoutTabLayout"
        android:layout_above="@+id/onboardingFragmentLayoutSloginButton" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Login"
        android:textColor="@color/white"
        android:id="@+id/onboardingFragmentLayoutSloginButton"
        android:layout_alignParentBottom="true"
        android:layout_above="@+id/onboardingFragmentLayoutViewPager"
        android:background="@android:color/transparent"/>
    
</RelativeLayout>